# Database
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/zorgdossier_dev"

# NextAuth
NEXTAUTH_SECRET="generate-a-random-secret-here-use-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# SMTP Email Configuration
SMTP_HOST="your-smtp-server.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASSWORD="your-smtp-password"
SMTP_FROM="noreply@zorgdossier.nl"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# System Settings (Fallback values if not configured in admin panel)
MAX_FILE_SIZE_MB="5"
SESSION_TIMEOUT_MINUTES="30"
